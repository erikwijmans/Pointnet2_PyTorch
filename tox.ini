[tox]
envlist =
    py36

[testenv]
# install pytest in the virtualenv where commands will be executed
deps =
    pytest
    numpy
    torch>=1.0
    git+git://github.com/erikwijmans/etw_pytorch_utils.git@v1.0.0#egg=etw_pytorch_utils-1.0.0
    pytest-helpers-namespace
commands =
    pytest
